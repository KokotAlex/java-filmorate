logging:
  level:
    ru:
      yandex.practicum.filmorate: debug
    org:
      springframework.web.servlet.DispatcherServlet: debug
server:
  port: 8080
spring:
  sql:
    init.mode: always
  datasource:
    # в jdbc-url укажите, что данные нужно сохранять в файл
    url: jdbc:h2:file:./db/filmorate
    driverClassName: org.h2.Driver
    username: sa
    password: password